server:
  port: 8080
  servlet:
    session:
      timeout: 30m
      cookie:
        http-only: true
        same-site: lax

spring:
  application:
    name: scyborsaUI
  thymeleaf:
    cache: false

api:
  base-url: http://localhost:8081
